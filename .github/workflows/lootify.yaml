name: lootify
on:
  workflow_dispatch:
  schedule:
    # CRON Generator Link: https://crontab.guru
    - cron: "33 9 * * *"

jobs:
  login-account:
    runs-on: ubuntu-latest
    name: login and lootify
    steps:
      - uses: elanthia-online/simu-rewards@v1
        with:
          account: ${{ secrets.ACCOUNT }}
          password: ${{ secrets.PASSWORD }}
          game: ${{ secrets.GAME_CODE }}
          bypass_login: false
