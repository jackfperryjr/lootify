name: lootify
on:
  workflow_dispatch
  # schedule:
  #   - chron: 0 7 5 * *
    
jobs:
  lootify:
    runs-on: ubuntu-latest
    steps:
      - name: pip install
        run: pip install -r requirements.txt
      # - name: gainsborough
      #   run: export user=${{ secrets.GAINSBOROUGH }} pass=${{ secrets.PASSWORD }}
      #   run: python3 lootcoins.py





